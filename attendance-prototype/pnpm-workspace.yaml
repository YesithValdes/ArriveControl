# pnpm ≥ 11: qué dependencias pueden ejecutar sus scripts de instalación.
# Sin esto, `pnpm install` se niega por estos paquetes y todo `pnpm add` falla.
allowBuilds:
  protobufjs: true
  sharp: true
